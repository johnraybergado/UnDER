# UnDER - Unsupervised Dense point cloud Extraction Routine
# Python 3.8+ required

--index-url https://pypi.org/simple
--extra-index-url https://download.pytorch.org/whl/cu118

# Core dependencies
numpy==1.26.4
pillow>=10.0.0

# PyTorch – automatically selects CUDA (Win/Linux) or CPU (macOS) via markers
torch==2.5.1+cu118 ; sys_platform == 'win32' or sys_platform == 'linux'
torchvision==0.20.1+cu118 ; sys_platform == 'win32' or sys_platform == 'linux'
torchaudio==2.5.1+cu118 ; sys_platform == 'win32' or sys_platform == 'linux'
torch==2.5.1 ; sys_platform == 'darwin'
torchvision==0.20.1 ; sys_platform == 'darwin'
torchaudio==2.5.1 ; sys_platform == 'darwin'

# Image processing
scikit-image>=0.21.0

# Data handling
pandas>=2.0.0

# Geospatial libraries (system GDAL required for Mac/Linux, wheel for Windows)
geopandas>=0.14.0
rasterio>=1.3.0
laspy>=2.0.0
plyfile>=0.8.0

# Database
psycopg2-binary>=2.9.0

# SGM (Semi-Global Matching) for disparity computation
pandora[sgm]>=1.8.1
joblib>=1.5.3
matplotlib>=3.8.10