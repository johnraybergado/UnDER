# Example commands to run UnDER pipeline on UseGeo Dataset-1
#
# The --base-image-filter parameter accepts:
#   - A single image filename to process only that image
#   - A comma-separated list of image filenames to process multiple images
#   - If not specified, all images in multiview_dicts.list will be processed
#   - Format of the multiline command is specific to Linux/Mac
#   - When running in Windows cmd replace "/" with "^" and for Windows powershell replace "/" with "`"

# Example 1: Process a single base image
python -m scripts.run_usegeo_pipeline \
  --data-root /path/to/dataset \
  --tmp-root /path/to/tmp_directory \
  --las-path /path/to/dataset/DIM_after_adjustment_dataset_C.las \
  --multiview-dicts /path/to/dataset/subset_multiview_dicts.list \
  --db-name ug1_subset \
  --disparity-method CNN \
  --model-path /path/to/dataset/PASMnet_KITTI2015_epoch80.pth \
  --disp-diff-threshold 0.75 \
  --base-image-filter 2021-04-23_13-17-22_S2223314_DxO.jpg

# Example 2: Process two base images (minimum for testing pipeline with multiple images)
python -m scripts.run_usegeo_pipeline \
  --data-root /path/to/dataset \
  --tmp-root /path/to/tmp_directory \
  --las-path /path/to/dataset/DIM_after_adjustment_dataset_C.las \
  --multiview-dicts /path/to/dataset/subset_multiview_dicts.list \
  --db-name ug1_subset \
  --disparity-method CNN \
  --model-path /path/to/dataset/PASMnet_KITTI2015_epoch80.pth \
  --disp-diff-threshold 0.75 \
  --base-image-filter 2021-04-23_13-17-22_S2223314_DxO.jpg,2021-04-23_13-17-28_S2223314_DxO.jpg

# Example 3: Process all images (no filter)
python -m scripts.run_usegeo_pipeline \
  --data-root /path/to/dataset \
  --tmp-root /path/to/tmp_directory \
  --las-path /path/to/dataset/DIM_after_adjustment_dataset_C.las \
  --multiview-dicts /path/to/dataset/subset_multiview_dicts.list \
  --db-name ug1_subset \
  --disparity-method CNN \
  --model-path /path/to/dataset/PASMnet_KITTI2015_epoch80.pth \
  --disp-diff-threshold 0.75

# Example 4: Use SGM disparity method (no model path needed)
python -m scripts.run_usegeo_pipeline \
  --data-root /path/to/dataset \
  --tmp-root /path/to/tmp_directory \
  --las-path /path/to/dataset/DIM_after_adjustment_dataset_C.las \
  --multiview-dicts /path/to/dataset/subset_multiview_dicts.list \
  --db-name ug1_subset \
  --disparity-method SGM \
  --disp-diff-threshold 0.75 \
  --base-image-filter 2021-04-23_13-17-22_S2223314_DxO.jpg,2021-04-23_13-17-28_S2223314_DxO.jpg
