[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "under_pipeline"
version = "0.1.0"

[tool.setuptools.packages.find]
include = ["under_pipeline*", "models*", "scripts*"]